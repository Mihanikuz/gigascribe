# Runtime dependencies of gigaam==0.1.0 installed before the package itself.
# Docker installs gigaam with --no-deps because its published metadata pins an
# obsolete torch<=2.5.1.  Keep torch and torchaudio out of this file.
hydra-core>=1.3,<1.4
omegaconf>=2.3,<2.4
sentencepiece>=0.1.99,<0.3
